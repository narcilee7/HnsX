// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: hnsx/v1/worker.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/hnsx-io/hnsx/server/proto/gen/go/hnsx/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// WorkerServiceName is the fully-qualified name of the WorkerService service.
	WorkerServiceName = "hnsx.v1.WorkerService"
	// SchedulerServiceName is the fully-qualified name of the SchedulerService service.
	SchedulerServiceName = "hnsx.v1.SchedulerService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// WorkerServiceRegisterProcedure is the fully-qualified name of the WorkerService's Register RPC.
	WorkerServiceRegisterProcedure = "/hnsx.v1.WorkerService/Register"
	// WorkerServiceHeartbeatProcedure is the fully-qualified name of the WorkerService's Heartbeat RPC.
	WorkerServiceHeartbeatProcedure = "/hnsx.v1.WorkerService/Heartbeat"
	// SchedulerServicePullSessionProcedure is the fully-qualified name of the SchedulerService's
	// PullSession RPC.
	SchedulerServicePullSessionProcedure = "/hnsx.v1.SchedulerService/PullSession"
	// SchedulerServiceAckSessionProcedure is the fully-qualified name of the SchedulerService's
	// AckSession RPC.
	SchedulerServiceAckSessionProcedure = "/hnsx.v1.SchedulerService/AckSession"
	// SchedulerServiceNackSessionProcedure is the fully-qualified name of the SchedulerService's
	// NackSession RPC.
	SchedulerServiceNackSessionProcedure = "/hnsx.v1.SchedulerService/NackSession"
	// SchedulerServiceStreamChannelProcedure is the fully-qualified name of the SchedulerService's
	// StreamChannel RPC.
	SchedulerServiceStreamChannelProcedure = "/hnsx.v1.SchedulerService/StreamChannel"
	// SchedulerServicePauseSessionProcedure is the fully-qualified name of the SchedulerService's
	// PauseSession RPC.
	SchedulerServicePauseSessionProcedure = "/hnsx.v1.SchedulerService/PauseSession"
	// SchedulerServiceResumeSessionProcedure is the fully-qualified name of the SchedulerService's
	// ResumeSession RPC.
	SchedulerServiceResumeSessionProcedure = "/hnsx.v1.SchedulerService/ResumeSession"
)

// WorkerServiceClient is a client for the hnsx.v1.WorkerService service.
type WorkerServiceClient interface {
	// Register is the first call after the worker process starts. The server
	// returns a canonical worker_id (it may reject duplicates or reassign) and
	// its preferred heartbeat cadence. Idempotent on retry.
	Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error)
	// Heartbeat carries the worker's resource usage and liveness signal. The
	// server may ack with a DrainHint asking the worker to drain.
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
}

// NewWorkerServiceClient constructs a client for the hnsx.v1.WorkerService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewWorkerServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) WorkerServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	workerServiceMethods := v1.File_hnsx_v1_worker_proto.Services().ByName("WorkerService").Methods()
	return &workerServiceClient{
		register: connect.NewClient[v1.RegisterRequest, v1.RegisterResponse](
			httpClient,
			baseURL+WorkerServiceRegisterProcedure,
			connect.WithSchema(workerServiceMethods.ByName("Register")),
			connect.WithClientOptions(opts...),
		),
		heartbeat: connect.NewClient[v1.HeartbeatRequest, v1.HeartbeatResponse](
			httpClient,
			baseURL+WorkerServiceHeartbeatProcedure,
			connect.WithSchema(workerServiceMethods.ByName("Heartbeat")),
			connect.WithClientOptions(opts...),
		),
	}
}

// workerServiceClient implements WorkerServiceClient.
type workerServiceClient struct {
	register  *connect.Client[v1.RegisterRequest, v1.RegisterResponse]
	heartbeat *connect.Client[v1.HeartbeatRequest, v1.HeartbeatResponse]
}

// Register calls hnsx.v1.WorkerService.Register.
func (c *workerServiceClient) Register(ctx context.Context, req *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error) {
	return c.register.CallUnary(ctx, req)
}

// Heartbeat calls hnsx.v1.WorkerService.Heartbeat.
func (c *workerServiceClient) Heartbeat(ctx context.Context, req *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return c.heartbeat.CallUnary(ctx, req)
}

// WorkerServiceHandler is an implementation of the hnsx.v1.WorkerService service.
type WorkerServiceHandler interface {
	// Register is the first call after the worker process starts. The server
	// returns a canonical worker_id (it may reject duplicates or reassign) and
	// its preferred heartbeat cadence. Idempotent on retry.
	Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error)
	// Heartbeat carries the worker's resource usage and liveness signal. The
	// server may ack with a DrainHint asking the worker to drain.
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
}

// NewWorkerServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewWorkerServiceHandler(svc WorkerServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	workerServiceMethods := v1.File_hnsx_v1_worker_proto.Services().ByName("WorkerService").Methods()
	workerServiceRegisterHandler := connect.NewUnaryHandler(
		WorkerServiceRegisterProcedure,
		svc.Register,
		connect.WithSchema(workerServiceMethods.ByName("Register")),
		connect.WithHandlerOptions(opts...),
	)
	workerServiceHeartbeatHandler := connect.NewUnaryHandler(
		WorkerServiceHeartbeatProcedure,
		svc.Heartbeat,
		connect.WithSchema(workerServiceMethods.ByName("Heartbeat")),
		connect.WithHandlerOptions(opts...),
	)
	return "/hnsx.v1.WorkerService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case WorkerServiceRegisterProcedure:
			workerServiceRegisterHandler.ServeHTTP(w, r)
		case WorkerServiceHeartbeatProcedure:
			workerServiceHeartbeatHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedWorkerServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedWorkerServiceHandler struct{}

func (UnimplementedWorkerServiceHandler) Register(context.Context, *connect.Request[v1.RegisterRequest]) (*connect.Response[v1.RegisterResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.WorkerService.Register is not implemented"))
}

func (UnimplementedWorkerServiceHandler) Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.WorkerService.Heartbeat is not implemented"))
}

// SchedulerServiceClient is a client for the hnsx.v1.SchedulerService service.
type SchedulerServiceClient interface {
	// PullSession long-polls: the server holds the call open until it has a
	// session to assign to this worker, or until max_wait_seconds elapses.
	// On timeout the server returns an empty PullSessionResponse.
	PullSession(context.Context, *connect.Request[v1.PullSessionRequest]) (*connect.Response[v1.PullSessionResponse], error)
	// AckSession flips the session row to "running" with the worker_id and
	// correlation_id. Sent after the worker has successfully spawned the
	// session subprocess and is about to begin Turn 1.
	AckSession(context.Context, *connect.Request[v1.AckSessionRequest]) (*connect.Response[v1.AckSessionResponse], error)
	// NackSession reports permanent failure on pickup (e.g. spec invalid in
	// the worker, sandbox backend missing). Server either marks session
	// "failed" (requeue=false) or re-queues it for another worker.
	NackSession(context.Context, *connect.Request[v1.NackSessionRequest]) (*connect.Response[v1.NackSessionResponse], error)
	// StreamChannel is the long-lived bidi channel. Worker streams up
	// StreamChannelRequest messages (oneof of observations / status / result).
	// Server pushes down StreamChannelResponse messages (oneof of cancel /
	// drain / domain-invalidation / pause / resume / ping).
	StreamChannel(context.Context) *connect.BidiStreamForClient[v1.StreamChannelRequest, v1.StreamChannelResponse]
	// PauseSession asks the server to flip a running session to "paused".
	// The session row stays bound to its worker; the worker is expected to
	// stop pulling new work for that session_id until ResumeSession is
	// received. The current step in flight is allowed to complete.
	PauseSession(context.Context, *connect.Request[v1.PauseSessionRequest]) (*connect.Response[v1.PauseSessionResponse], error)
	// ResumeSession flips a paused session back to "running" so the
	// worker resumes pulling work for it.
	ResumeSession(context.Context, *connect.Request[v1.ResumeSessionRequest]) (*connect.Response[v1.ResumeSessionResponse], error)
}

// NewSchedulerServiceClient constructs a client for the hnsx.v1.SchedulerService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSchedulerServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SchedulerServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	schedulerServiceMethods := v1.File_hnsx_v1_worker_proto.Services().ByName("SchedulerService").Methods()
	return &schedulerServiceClient{
		pullSession: connect.NewClient[v1.PullSessionRequest, v1.PullSessionResponse](
			httpClient,
			baseURL+SchedulerServicePullSessionProcedure,
			connect.WithSchema(schedulerServiceMethods.ByName("PullSession")),
			connect.WithClientOptions(opts...),
		),
		ackSession: connect.NewClient[v1.AckSessionRequest, v1.AckSessionResponse](
			httpClient,
			baseURL+SchedulerServiceAckSessionProcedure,
			connect.WithSchema(schedulerServiceMethods.ByName("AckSession")),
			connect.WithClientOptions(opts...),
		),
		nackSession: connect.NewClient[v1.NackSessionRequest, v1.NackSessionResponse](
			httpClient,
			baseURL+SchedulerServiceNackSessionProcedure,
			connect.WithSchema(schedulerServiceMethods.ByName("NackSession")),
			connect.WithClientOptions(opts...),
		),
		streamChannel: connect.NewClient[v1.StreamChannelRequest, v1.StreamChannelResponse](
			httpClient,
			baseURL+SchedulerServiceStreamChannelProcedure,
			connect.WithSchema(schedulerServiceMethods.ByName("StreamChannel")),
			connect.WithClientOptions(opts...),
		),
		pauseSession: connect.NewClient[v1.PauseSessionRequest, v1.PauseSessionResponse](
			httpClient,
			baseURL+SchedulerServicePauseSessionProcedure,
			connect.WithSchema(schedulerServiceMethods.ByName("PauseSession")),
			connect.WithClientOptions(opts...),
		),
		resumeSession: connect.NewClient[v1.ResumeSessionRequest, v1.ResumeSessionResponse](
			httpClient,
			baseURL+SchedulerServiceResumeSessionProcedure,
			connect.WithSchema(schedulerServiceMethods.ByName("ResumeSession")),
			connect.WithClientOptions(opts...),
		),
	}
}

// schedulerServiceClient implements SchedulerServiceClient.
type schedulerServiceClient struct {
	pullSession   *connect.Client[v1.PullSessionRequest, v1.PullSessionResponse]
	ackSession    *connect.Client[v1.AckSessionRequest, v1.AckSessionResponse]
	nackSession   *connect.Client[v1.NackSessionRequest, v1.NackSessionResponse]
	streamChannel *connect.Client[v1.StreamChannelRequest, v1.StreamChannelResponse]
	pauseSession  *connect.Client[v1.PauseSessionRequest, v1.PauseSessionResponse]
	resumeSession *connect.Client[v1.ResumeSessionRequest, v1.ResumeSessionResponse]
}

// PullSession calls hnsx.v1.SchedulerService.PullSession.
func (c *schedulerServiceClient) PullSession(ctx context.Context, req *connect.Request[v1.PullSessionRequest]) (*connect.Response[v1.PullSessionResponse], error) {
	return c.pullSession.CallUnary(ctx, req)
}

// AckSession calls hnsx.v1.SchedulerService.AckSession.
func (c *schedulerServiceClient) AckSession(ctx context.Context, req *connect.Request[v1.AckSessionRequest]) (*connect.Response[v1.AckSessionResponse], error) {
	return c.ackSession.CallUnary(ctx, req)
}

// NackSession calls hnsx.v1.SchedulerService.NackSession.
func (c *schedulerServiceClient) NackSession(ctx context.Context, req *connect.Request[v1.NackSessionRequest]) (*connect.Response[v1.NackSessionResponse], error) {
	return c.nackSession.CallUnary(ctx, req)
}

// StreamChannel calls hnsx.v1.SchedulerService.StreamChannel.
func (c *schedulerServiceClient) StreamChannel(ctx context.Context) *connect.BidiStreamForClient[v1.StreamChannelRequest, v1.StreamChannelResponse] {
	return c.streamChannel.CallBidiStream(ctx)
}

// PauseSession calls hnsx.v1.SchedulerService.PauseSession.
func (c *schedulerServiceClient) PauseSession(ctx context.Context, req *connect.Request[v1.PauseSessionRequest]) (*connect.Response[v1.PauseSessionResponse], error) {
	return c.pauseSession.CallUnary(ctx, req)
}

// ResumeSession calls hnsx.v1.SchedulerService.ResumeSession.
func (c *schedulerServiceClient) ResumeSession(ctx context.Context, req *connect.Request[v1.ResumeSessionRequest]) (*connect.Response[v1.ResumeSessionResponse], error) {
	return c.resumeSession.CallUnary(ctx, req)
}

// SchedulerServiceHandler is an implementation of the hnsx.v1.SchedulerService service.
type SchedulerServiceHandler interface {
	// PullSession long-polls: the server holds the call open until it has a
	// session to assign to this worker, or until max_wait_seconds elapses.
	// On timeout the server returns an empty PullSessionResponse.
	PullSession(context.Context, *connect.Request[v1.PullSessionRequest]) (*connect.Response[v1.PullSessionResponse], error)
	// AckSession flips the session row to "running" with the worker_id and
	// correlation_id. Sent after the worker has successfully spawned the
	// session subprocess and is about to begin Turn 1.
	AckSession(context.Context, *connect.Request[v1.AckSessionRequest]) (*connect.Response[v1.AckSessionResponse], error)
	// NackSession reports permanent failure on pickup (e.g. spec invalid in
	// the worker, sandbox backend missing). Server either marks session
	// "failed" (requeue=false) or re-queues it for another worker.
	NackSession(context.Context, *connect.Request[v1.NackSessionRequest]) (*connect.Response[v1.NackSessionResponse], error)
	// StreamChannel is the long-lived bidi channel. Worker streams up
	// StreamChannelRequest messages (oneof of observations / status / result).
	// Server pushes down StreamChannelResponse messages (oneof of cancel /
	// drain / domain-invalidation / pause / resume / ping).
	StreamChannel(context.Context, *connect.BidiStream[v1.StreamChannelRequest, v1.StreamChannelResponse]) error
	// PauseSession asks the server to flip a running session to "paused".
	// The session row stays bound to its worker; the worker is expected to
	// stop pulling new work for that session_id until ResumeSession is
	// received. The current step in flight is allowed to complete.
	PauseSession(context.Context, *connect.Request[v1.PauseSessionRequest]) (*connect.Response[v1.PauseSessionResponse], error)
	// ResumeSession flips a paused session back to "running" so the
	// worker resumes pulling work for it.
	ResumeSession(context.Context, *connect.Request[v1.ResumeSessionRequest]) (*connect.Response[v1.ResumeSessionResponse], error)
}

// NewSchedulerServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSchedulerServiceHandler(svc SchedulerServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	schedulerServiceMethods := v1.File_hnsx_v1_worker_proto.Services().ByName("SchedulerService").Methods()
	schedulerServicePullSessionHandler := connect.NewUnaryHandler(
		SchedulerServicePullSessionProcedure,
		svc.PullSession,
		connect.WithSchema(schedulerServiceMethods.ByName("PullSession")),
		connect.WithHandlerOptions(opts...),
	)
	schedulerServiceAckSessionHandler := connect.NewUnaryHandler(
		SchedulerServiceAckSessionProcedure,
		svc.AckSession,
		connect.WithSchema(schedulerServiceMethods.ByName("AckSession")),
		connect.WithHandlerOptions(opts...),
	)
	schedulerServiceNackSessionHandler := connect.NewUnaryHandler(
		SchedulerServiceNackSessionProcedure,
		svc.NackSession,
		connect.WithSchema(schedulerServiceMethods.ByName("NackSession")),
		connect.WithHandlerOptions(opts...),
	)
	schedulerServiceStreamChannelHandler := connect.NewBidiStreamHandler(
		SchedulerServiceStreamChannelProcedure,
		svc.StreamChannel,
		connect.WithSchema(schedulerServiceMethods.ByName("StreamChannel")),
		connect.WithHandlerOptions(opts...),
	)
	schedulerServicePauseSessionHandler := connect.NewUnaryHandler(
		SchedulerServicePauseSessionProcedure,
		svc.PauseSession,
		connect.WithSchema(schedulerServiceMethods.ByName("PauseSession")),
		connect.WithHandlerOptions(opts...),
	)
	schedulerServiceResumeSessionHandler := connect.NewUnaryHandler(
		SchedulerServiceResumeSessionProcedure,
		svc.ResumeSession,
		connect.WithSchema(schedulerServiceMethods.ByName("ResumeSession")),
		connect.WithHandlerOptions(opts...),
	)
	return "/hnsx.v1.SchedulerService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SchedulerServicePullSessionProcedure:
			schedulerServicePullSessionHandler.ServeHTTP(w, r)
		case SchedulerServiceAckSessionProcedure:
			schedulerServiceAckSessionHandler.ServeHTTP(w, r)
		case SchedulerServiceNackSessionProcedure:
			schedulerServiceNackSessionHandler.ServeHTTP(w, r)
		case SchedulerServiceStreamChannelProcedure:
			schedulerServiceStreamChannelHandler.ServeHTTP(w, r)
		case SchedulerServicePauseSessionProcedure:
			schedulerServicePauseSessionHandler.ServeHTTP(w, r)
		case SchedulerServiceResumeSessionProcedure:
			schedulerServiceResumeSessionHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSchedulerServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSchedulerServiceHandler struct{}

func (UnimplementedSchedulerServiceHandler) PullSession(context.Context, *connect.Request[v1.PullSessionRequest]) (*connect.Response[v1.PullSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.SchedulerService.PullSession is not implemented"))
}

func (UnimplementedSchedulerServiceHandler) AckSession(context.Context, *connect.Request[v1.AckSessionRequest]) (*connect.Response[v1.AckSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.SchedulerService.AckSession is not implemented"))
}

func (UnimplementedSchedulerServiceHandler) NackSession(context.Context, *connect.Request[v1.NackSessionRequest]) (*connect.Response[v1.NackSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.SchedulerService.NackSession is not implemented"))
}

func (UnimplementedSchedulerServiceHandler) StreamChannel(context.Context, *connect.BidiStream[v1.StreamChannelRequest, v1.StreamChannelResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.SchedulerService.StreamChannel is not implemented"))
}

func (UnimplementedSchedulerServiceHandler) PauseSession(context.Context, *connect.Request[v1.PauseSessionRequest]) (*connect.Response[v1.PauseSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.SchedulerService.PauseSession is not implemented"))
}

func (UnimplementedSchedulerServiceHandler) ResumeSession(context.Context, *connect.Request[v1.ResumeSessionRequest]) (*connect.Response[v1.ResumeSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hnsx.v1.SchedulerService.ResumeSession is not implemented"))
}
