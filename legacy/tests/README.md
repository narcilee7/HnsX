# Integration tests

Integration tests will live here. Empty for the v0.1.0 skeleton — the first
end-to-end test should land as `crates/hnsx-cli/tests/cli_smoke.rs` or
`crates/hnsx-core/tests/round_trip.rs` (DomainSpec YAML parsing).
